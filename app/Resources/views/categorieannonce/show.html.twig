{% extends 'base.html.twig' %}

{% block body %}
    <h1>Categorieannonce</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorieAnnonce.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ categorieAnnonce.nom }}</td>
            </tr>
            <tr>
                <th>Class</th>
                <td>{{ categorieAnnonce.class }}</td>
            </tr>
            <tr>
                <th>Icon</th>
                <td>{{ categorieAnnonce.icon }}</td>
            </tr>
            <tr>
                <th>IconMini</th>
                <td>{{ categorieAnnonce.iconMini }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('categorieannonce_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('categorieannonce_edit', { 'id': categorieAnnonce.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
